import {} from '~/puzzle/day-4'
import { getInput } from '~/inputs'

describe('Part 1', () => {
  it('should answer example', () => {
    const data = ``
  })

  it('should answer puzzle', () => {
    const data = getInput('day-4')
  })
})

describe('Part 2', () => {
  it('should answer example', () => {
    const data = ``
  })

  it('should answer puzzle', () => {
    const data = getInput('day-4')
  })
})
