import {} from '~/puzzle/day-1'
import { getInput } from '~/inputs'

describe('Part 1', () => {
  it('should answer example', () => {
    const data = ``
  })

  it('should answer puzzle', () => {
    const data = getInput('day-1')
  })
})

describe('Part 2', () => {
  it('should answer example', () => {
    const data = ``
  })

  it('should answer puzzle', () => {
    const data = getInput('day-1')
  })
})
